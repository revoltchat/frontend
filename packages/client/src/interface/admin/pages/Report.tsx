import { Typography } from "@revolt/ui";
import { TabProps } from "../Admin";

export function Report(props: TabProps<"reports">) {
  return <Typography variant="legacy-settings-title">Report</Typography>;
}
