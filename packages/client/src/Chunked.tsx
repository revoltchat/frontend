import { Button } from "@revolt/ui/components/design/atoms/inputs/Button";

export default function T() {
  return (
    <Button variant="secondary">
      look, im a lazy component! and i did not shitted like styled components!
    </Button>
  );
}
